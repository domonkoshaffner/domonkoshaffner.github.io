{
  "name": "domonkos-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-privacy.mjs --source && astro build && node scripts/check-privacy.mjs --build",
    "check:privacy": "node scripts/check-privacy.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "@fontsource/michroma": "^5.3.0",
    "astro": "^7.3.2"
  },
  "allowScripts": {
    "esbuild": true
  }
}
